{
  "id": "wordsmith",
  "version": "1.0.0",
  "description": "Wordsmith FoundryVTT module",
  "main": "scripts/wordsmith.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
